import { Meta } from '@storybook/blocks';

<Meta title="src/@types/controls" />

## EventItem
`src/@types/controls`

**AST Node Type:** *InterfaceDeclaration*

#### JSDoc annotations:

Describes an event item with a name and trigger.

```bash
@interface EventItem - The interface name

@property {string} name - The event name

@property {string} trigger - The event trigger
```

#### Code:

```ts
export interface EventItem {
    name: string;
    trigger: string;
}

```

